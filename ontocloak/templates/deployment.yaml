kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Release.Name }}-ontocloak-deployment
  {{- with .Values.ontocloak.deployment.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.ontocloak.deployment.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.ontocloak.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-ontocloak
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-ontocloak
        {{- with .Values.ontocloak.deployment.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.ontocloak.deployment.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- if $.Values.ontocloak.deployment.imagePullSecrets  }}
      imagePullSecrets: {{ toJson .Values.ontocloak.deployment.imagePullSecrets }}
      {{- end }}
      {{- if not .Values.ontocloak.migration.skipCheck }}
      initContainers:
        # 1 – DB check
        - name: db-check
          image: postgres:{{ .Values.ontocloak.deployment.db.provided.postgresVersion }}
          command: ["/bin/bash", "-c"]
          env:
            - name: TZ
              value: {{ .Values.ontocloak.timeZone }}
            - name: KC_REALM_NAME
              value: {{ .Values.ontocloak.migration.realmName }}
            - name: KC_DB_SCHEMA
              value: public
            - name: KC_DB_URL_HOST
            {{- if .Values.ontocloak.deployment.db.external.enabled }}
              value: {{ .Values.ontocloak.deployment.db.external.hostName }}
            {{- else }}
              value: {{ .Release.Name }}-ontocloak-db-service
            {{- end }}
            - name: KC_DB_URL_DATABASE
              value: {{ .Values.ontocloak.deployment.db.database }}
            - name: KC_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.dbSecretName" . }}
                  key: {{ include "ontocloak.dbUserKey" . }}
            - name: KC_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.dbSecretName" . }}
                  key: {{ include "ontocloak.dbPasswordKey" . }}
          args:
            - |
              set -e
              # wait up to ~2m for the DB to be reachable
              ready=false
              for i in {1..12}; do
                if PGPASSWORD="$KC_DB_PASSWORD" \
                  psql -h "$KC_DB_URL_HOST" -U "$KC_DB_USERNAME" -d "$KC_DB_URL_DATABASE" -c '\q'; then
                  echo "DB is up!" && ready=true && break
                fi
                echo "Waiting for DB… ($i/12)" && sleep 5
              done
              if ! $ready; then
                echo "ERROR: DB not ready after 60s" >&2
                exit 1
              fi
              # now check for the realm
              if PGPASSWORD="$KC_DB_PASSWORD" \
                psql -h "$KC_DB_URL_HOST" -U "$KC_DB_USERNAME" -d "$KC_DB_URL_DATABASE" \
                  -tAc "SELECT 1 FROM $KC_DB_SCHEMA.realm WHERE name='${KC_REALM_NAME}' LIMIT 1;" \
                | grep -q 1; then
                echo "Realm exists – skipping import" && mkdir -p /work && touch /work/skip
              fi
          volumeMounts:
            - name: workdir
              mountPath: /work
        # 2 – Import if realm doesn't exist in DB
        - name: realm-import
          image: {{ .Values.ontocloak.deployment.image }}
          env:
            - name: TZ
              value: {{ .Values.ontocloak.timeZone }}
            - name: KC_DB_URL_HOST
            {{- if .Values.ontocloak.deployment.db.external.enabled }}
              value: {{ .Values.ontocloak.deployment.db.external.hostName }}
            {{- else }}
              value: {{ .Release.Name }}-ontocloak-db-service
            {{- end }}
            - name: KC_DB_URL_DATABASE
              value: {{ .Values.ontocloak.deployment.db.database }}
            - name: KC_DB_SCHEMA
              value: public
            - name: KC_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.dbSecretName" . }}
                  key: {{ include "ontocloak.dbUserKey" . }}
            - name: KC_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.dbSecretName" . }}
                  key: {{ include "ontocloak.dbPasswordKey" . }}
            - name: KC_BOOTSTRAP_ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.adminSecretName" . }}
                  key: {{ include "ontocloak.adminUserKey" . }}
            - name: KC_BOOTSTRAP_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.adminSecretName" . }}
                  key: {{ include "ontocloak.adminPasswordKey" . }}
          command: ["/bin/bash", "-c"]
          args:
            - |
              if [ -f /work/skip ]; then echo "Realm exist in database! Skipping import..."; else {{ .Values.ontocloak.deployment.kc_sh_path }} import --file=/opt/keycloak/data/import/realm.json --override --hostname={{ .Values.ontocloak.serverName }} --proxy edge --optimized; fi
          volumeMounts:
            - name: workdir
              mountPath: /work
            - name: {{ .Release.Name }}-ontocloak-realm-config
              mountPath: /opt/keycloak/data/import
      {{- end }}
      containers:
        - name: {{ .Release.Name }}-ontocloak
          image: {{ required "An ontocloak.deployment.image value is required" .Values.ontocloak.deployment.image }}
          imagePullPolicy: Always
          ports:
           - name: http
             containerPort: 8080
           {{- if .Values.ontocloak.metrics.enabled }}
           - name: management
             containerPort: 9000
           {{- end }}
          livenessProbe:
            httpGet:
              path: /auth/health/live
              port: 9000
            initialDelaySeconds: 50
            periodSeconds: 5
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /auth/health/ready
              port: 9000
            initialDelaySeconds: 0
            periodSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /auth/health/started
              port: 9000
            initialDelaySeconds: 0
            periodSeconds: 5
            failureThreshold: 120
          resources:
            requests:
              cpu: {{ .Values.ontocloak.deployment.resources.requests.cpu }}
              memory: {{ .Values.ontocloak.deployment.resources.requests.memory }}
            limits:
              cpu: {{ .Values.ontocloak.deployment.resources.limits.cpu }}
              memory: {{ .Values.ontocloak.deployment.resources.limits.memory }}
          args:
            - start
            - --hostname={{ .Values.ontocloak.serverName }}
            - --proxy-headers=xforwarded
            - --http-enabled=true
            - --optimized
          env:
            - name: TZ
              value: {{ .Values.ontocloak.timeZone }}
            - name: KC_DB_URL_HOST
            {{- if .Values.ontocloak.deployment.db.external.enabled }}
              value: {{ .Values.ontocloak.deployment.db.external.hostName }}
            {{- else }}
              value: {{ .Release.Name }}-ontocloak-db-service
            {{- end }}
            - name: KC_DB_URL_DATABASE
              value: {{ .Values.ontocloak.deployment.db.database }}
            - name: KC_DB_SCHEMA
              value: public
            - name: KC_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.dbSecretName" . }}
                  key: {{ include "ontocloak.dbUserKey" . }}
            - name: KC_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.dbSecretName" . }}
                  key: {{ include "ontocloak.dbPasswordKey" . }}
            - name: KC_BOOTSTRAP_ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.adminSecretName" . }}
                  key: {{ include "ontocloak.adminUserKey" . }}
            - name: KC_BOOTSTRAP_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ontocloak.adminSecretName" . }}
                  key: {{ include "ontocloak.adminPasswordKey" . }}
            {{- range $key, $value := .Values.ontocloak.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          volumeMounts:
            - name: {{ .Release.Name }}-ontocloak-properties
              mountPath: /opt/keycloak/conf/agreements.properties
              subPath: ontocloak.properties
            {{- if .Values.ontocloak.customisation }}
            - name: {{ .Release.Name }}-ontocloak-admin-customisation
              mountPath: /opt/keycloak/themes/ontocloak/admin/resources/img/ontoserver-logo.png
              subPath:  ontoserver-logo.png
            - name: {{ .Release.Name }}-ontocloak-login-customisation
              mountPath: /opt/keycloak/themes/ontocloak/login
            {{- end}}
      volumes:
        {{- if not .Values.ontocloak.migration.skipCheck }}
        - name: {{ .Release.Name }}-ontocloak-realm-config
          configMap:
            name: {{ .Release.Name }}-ontocloak-realm-config
        - name: workdir
          emptyDir: {}
        {{- end }}
        - name: {{ .Release.Name }}-ontocloak-properties
          configMap:
            name: {{ .Release.Name }}-ontocloak-properties
      {{- if .Values.ontocloak.deployment.tolerations }}
      tolerations: {{ toJson .Values.ontocloak.deployment.tolerations }}
      {{- end }}
{{- if and (not .Values.ontocloak.admin.existingSecret.enabled) (not .Values.ontocloak.admin.externalSecret.enabled) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ontocloak.adminSecretName" . }}
data:
  {{ include "ontocloak.adminUserKey" . }}: {{ .Values.ontocloak.admin.user | b64enc }}
  {{ include "ontocloak.adminPasswordKey" . }}: {{ .Values.ontocloak.admin.password | b64enc }}
{{- end }}
